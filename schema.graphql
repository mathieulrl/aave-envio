type Position {
  id: ID!
  chainId: Int!
  token: String! # always aEURe for now
  walletAddress: String!
  depositedBalance: BigInt!
  lastBalance: BigInt!
  createdAt: String!
  updatedAt: String!
}

